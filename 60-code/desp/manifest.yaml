# artefact_type: descriptor
# visibility: public
# audience: automation
# form: yaml
# role: manifest
# status: operative
# owner: engineering-core

# ------------------------------------------------------------------------------
# DISCLAIMER: ILLUSTRATIVE ARTEFACT
# ------------------------------------------------------------------------------
# The code and configurations in this folder are illustrative and educational.
# They do not represent official implementations, production-ready components,
# or endorsed technical approaches for the Digital Euro or any other real-world
# system.
# ------------------------------------------------------------------------------

metadata:
  id: COMP-DESP-CORE
  name: "Digital Euro Service Platform (Core)"
  description: "The central ledger and settlement engine for the Digital Euro scheme."
  version: "0.1.0"
  
owner:
  institution: "Eurosystem"
  team: "settlement-engine-team"
  contact: "desp-maintainers@eurosystem.int"

# The specific specifications this component realizes
implements:
  - spec_id: "SPEC-LEDGER-001"
    version: "1.2.0"
    coverage: "partial"
  - spec_id: "SPEC-TOKEN-050"
    version: "1.0.0"

# Interfaces exposed to other components
interfaces:
  exposed:
    - id: "API-DESP-SETTLEMENT-GRPC"
      type: "grpc"
      protocol_version: "v2"
    - id: "API-DESP-LOOKUP-HTTP"
      type: "rest"
      openapi_ref: "/docs/openapi/lookup-v1.yaml"

  consumed:
    - id: "API-ALIAS-LOOKUP"
      provider: "COMP-ALIAS-SERVICE"

# Links to assurance artefacts (tests) that verify this component
assurance:
  - test_suite_id: "TEST-SET-LEDGER-CORE"
    location: "../60-tests/400-operational/system-level"

    