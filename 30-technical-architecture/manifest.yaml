# artefact_type: architecture
# visibility: public
# audience: everyone
# form: text
# role: manifest
# status: operative
# owner: technical-architecture

meta:
  # Unique Identifier for the (Technical) Architecture Set.
  # Kept as SET-ARCH so downstream specs continue to anchor with @arch=SET-ARCH.
  id: SET-ARCH

  # Human-readable title
  title: "Technical Architecture"

  # The Normative Version
  version: "0.1.0"

  # The Lifecycle State
  status: FICTIONAL-RELEASED

  # The Freeze Date
  date: 2025-12-29

# The explicit list of files that constitute this version
contains:
  - README.md
  - component-inventory.md
